import React, { FC } from 'react';
interface IProps {
    className: string;
    flexDirection: string;
    id?: string;
    padding?: string;
    style?: React.CSSProperties;
}
declare const Card: FC<IProps>;
export default Card;
