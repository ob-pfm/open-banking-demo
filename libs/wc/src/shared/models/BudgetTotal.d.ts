import Budget from './Budget';
export default class BudgetTotal {
    amountTotal: number;
    spentAmountTotal: number;
    data: Budget[];
    constructor(amountTotal: number, spentAmountTotal: number, data: Budget[]);
}
