import { ListAction } from '../types/ListAction';
import { ListState } from '../types/ListState';
declare const ListReducer: (state: ListState, action: ListAction) => ListState;
export default ListReducer;
